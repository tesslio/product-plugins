# storefront

Order capture and payments. The API client under `src/api/generated/` is
regenerated from the payments provider's OpenAPI document by `pnpm codegen`, and
committed so builds do not need network access.
