# Stop repeated generated-client findings

Use `update-code-review-package` with `evidence.md` as the complete bounded
evidence set. Update the active package only where the repeated evidence and
repository paths justify it. Preserve unrelated coverage and write
`result-notes.md` with the outcome, evidence coverage, and validation limits.
