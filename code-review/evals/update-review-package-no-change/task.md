# Check whether our review package needs an update

Use `update-code-review-package`. `evidence.md` is the full bounded evidence set.
Make no edit unless the evidence justifies one. Write `result-notes.md` with the
outcome, coverage, and validation limitations.
