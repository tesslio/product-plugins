{
  "description": "Fork a default lens into a repository-local variant, dropping one dimension and adding one with its own bar and exclusion, and state how the full lens set is selected once the fork exists"
}
